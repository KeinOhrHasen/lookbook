'use client';

export default function Error(error) {
  return (
    <div className="flex flex-col items-center justify-center space-y-3 w-full h-full p-10">
      Something went wrong with albums
    </div>
  );
}
